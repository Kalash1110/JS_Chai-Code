//For loop 
for (let index = 0; index < 10; index++) {
    console.log(index);
    
    
}

let myArray=["Kalash","Yashi","Patu","Sristy"]

for(let idx=0;idx<myArray.length;idx++)
    console.log(myArray[idx]);

    //Note if your write idx<=myArray.length,you won't get error,you will get value printed as undefined as there are no out of bound exceptions in JS


//Break and continue are similar to java